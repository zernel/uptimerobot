<% content_for(:page_title) { "Edit #{@maintenance_window.name}" } %>

<div class="mx-auto max-w-3xl">
  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <h2 class="text-lg font-semibold text-slate-900">Edit Maintenance Window</h2>

    <div class="mt-6">
      <%= render "form", maintenance_window: @maintenance_window, monitors: @monitors %>
    </div>
  </div>
</div>
